# ============================================
# GDash - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha com seus valores
# cp .env.example .env

# ============================================
# API (NestJS)
# ============================================
# URI de conexão com MongoDB
# Formato: mongodb://usuario:senha@host:porta/database?authSource=admin
MONGODB_URI=mongodb://admin:admin123@localhost:27017/gdash?authSource=admin

# Porta em que a API irá rodar
PORT=3000

# Ambiente de execução (development, production)
NODE_ENV=development

# Chave secreta para assinatura de tokens JWT
# IMPORTANTE: Altere para um valor seguro em produção!
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Tempo de expiração do token JWT (ex: 7d, 24h, 1h)
JWT_EXPIRES_IN=7d

# URL de conexão com RabbitMQ
# Formato CloudAMQP: amqps://usuario:senha@host.cloudamqp.com/usuario
# Formato local: amqp://admin:admin123@localhost:5672
RABBITMQ_URL=amqps://usuario:senha@host.cloudamqp.com/usuario

# ============================================
# Collector (Python)
# ============================================
# URL de conexão com RabbitMQ
# Formato CloudAMQP: amqps://usuario:senha@host.cloudamqp.com/usuario
# Formato local: amqp://admin:admin123@localhost:5672
# COLLECTOR_RABBITMQ_URL=amqps://usuario:senha@host.cloudamqp.com/usuario

# Nome da fila no RabbitMQ (será criada automaticamente se não existir)
QUEUE_NAME=weather.readings

# Intervalo de coleta em segundos (3600 = 1 hora)
COLLECTION_INTERVAL=3600

# ============================================
# Worker (Go)
# ============================================
# URL de conexão com RabbitMQ
# Formato CloudAMQP: amqps://usuario:senha@host.cloudamqp.com/usuario
# Formato local: amqp://admin:admin123@localhost:5672
# WORKER_RABBITMQ_URL=amqps://usuario:senha@host.cloudamqp.com/usuario

# URL da API NestJS
# Quando rodando via Docker Compose: http://api:3000
# Quando rodando localmente: http://localhost:3000
API_URL=http://localhost:3000

# ============================================
# Frontend (React/Vite)
# ============================================
# URL da API NestJS para o frontend
# Quando rodando via Docker Compose: http://localhost:3000
# Quando rodando localmente: http://localhost:3000
VITE_API_URL=http://localhost:3000

# ============================================
# MongoDB (quando rodando localmente)
# ============================================
# Credenciais do MongoDB (usadas apenas se rodar MongoDB localmente)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=admin123
MONGO_INITDB_DATABASE=gdash

# ============================================
# RabbitMQ (quando rodando localmente)
# ============================================
# Credenciais do RabbitMQ (usadas apenas se rodar RabbitMQ localmente)
# RABBITMQ_DEFAULT_USER=admin
# RABBITMQ_DEFAULT_PASS=admin123
